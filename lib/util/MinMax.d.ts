export declare class MinMax {
    private _min;
    private _max;
    private _defaultMin;
    private _defaultMax;
    constructor(_min: number, _max: number, _defaultMin?: number, _defaultMax?: number);
}
